dotnet build initially failed because FilmAPI.exe was locked by a running process; stopping the process resolved migration generation and build verification.
LSP diagnostics could not run in this session because the configured biome and typescript-language-server binaries are not installed in the environment.
